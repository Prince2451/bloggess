const apiUrls = {
  auth: {
    refreshToken: "/auth/token",
    login: "/auth/login",
    register: "/auth/register",
    user: "/auth/user",
  },
  posts: {
    all: "/posts",
  },
};

export default apiUrls;
